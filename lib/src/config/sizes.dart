class AppSizes {
  static final AppSizes _appColors = AppSizes._internal();

  factory AppSizes() {
    return _appColors;
  }

  AppSizes._internal();

  static const double defaultPadding = 20.0;
}
